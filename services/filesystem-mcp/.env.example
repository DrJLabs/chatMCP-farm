# --- Keycloak / OAuth ---
# Keep confidential client credentials inside ../../.keycloak-env and source them before running compose scripts.
KEYCLOAK_REALM=OMA
FILESYSTEM_MCP_CLIENT_ID=filesystem-mcp
FILESYSTEM_MCP_CLIENT_NAME_HUMAN="Filesystem MCP Bridge"
FILESYSTEM_MCP_CLIENT_NAME_MODEL=filesystem-mcp
FILESYSTEM_MCP_CLIENT_DESCRIPTION="Streams approved host roots to MCP clients via SSE bridge"
# Update the issuer and public URLs to match your deployment footprint.
OIDC_ISSUER=https://oauth.example.com/auth/realms/OMA
OIDC_AUDIENCE=https://fs-mcp.local/mcp

# --- Compose Profile Activation ---
# Set COMPOSE_PROFILES=filesystem-mcp when running scripts/compose.sh to keep this bridge opt-in.
MCP_BUILD_CONTEXT=..
MCP_DOCKERFILE=services/filesystem-mcp/Bridge.Dockerfile
MCP_NETWORK_EXTERNAL=traefik
MCP_CPU_LIMIT=0.50
MCP_MEMORY_LIMIT=256M
MCP_MEMORY_RESERVATION=128M
MCP_PUBLIC_HOST=fs-mcp.local
SSE_PORT=12010

# --- Host Mounts & Security Toggles ---
# Defaults mount read-only directories; adjust only after PO/QA approval.
HOST_PROJECTS_ROOT=/srv/projects
HOST_VAULTS_ROOT=/srv/vaults
PROJECTS_READ_ONLY=true
VAULTS_READ_ONLY=true
FS_ALLOWED=/projects:/VAULTS
ALLOW_WRITE=false
ENABLE_ROOTS=false

# When enabling writes set PROJECTS_READ_ONLY=false and/or VAULTS_READ_ONLY=false alongside ALLOW_WRITE=true.
# Capture warning-banner evidence from container logs before promoting to shared environments.

# --- Optional Overrides ---
# MCP_ALLOWED_ORIGINS=https://chat.openai.com,https://chatgpt.com
# TRAEFIK_TLS_RESOLVER=letsencrypt

# Reference the shared Keycloak helper if present
# source ../../.keycloak-env
