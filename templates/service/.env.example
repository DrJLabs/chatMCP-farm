# Canonical MCP service environment template.
# Copy to services/<service>/.env and adjust values per service.

## Shared defaults
# Set PORT in the real .env once you pick an available port for this service
# PORT=
REQUIRE_AUTH=true
ENABLE_STREAMABLE=true
ENABLE_SSE=true
ALLOWED_ORIGINS=https://chatgpt.com,https://chat.openai.com
DEBUG_HEADERS=false
MCP_NETWORK_EXTERNAL=traefik
# Optional: uncomment to attach a second Docker network (e.g. private bridge)
# MCP_NETWORK_INTERNAL=service-internal
# MCP_NETWORK_INTERNAL_EXTERNAL=false
# MCP_NETWORK_INTERNAL_INTERNAL=true
# (extend the service compose file if you need to connect this network)
MCP_PUBLIC_HOST=service.example.com
MCP_SERVICE_NAME=service-mcp

## OAuth / resource configuration
MCP_PUBLIC_BASE_URL=https://service.example.com/mcp
PRM_RESOURCE_URL=https://service.example.com/mcp
OIDC_ISSUER=https://keycloak.example.com/auth/realms/example
OIDC_AUDIENCE=https://service.example.com/mcp

## Optional service metadata
MCP_SERVER_NAME=mcpserver
MCP_SERVER_VERSION=0.1.0

## Downstream API configuration (override per service)
# BACKEND_BASE_URL=https://backend.example.com
# BACKEND_API_TOKEN=changeme
