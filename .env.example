# Environment variables for MCP servers workspace
# Copy to .env and customise per service before running docker compose.

# Shared defaults
NODE_ENV=development
ENABLE_STREAMABLE=true
REQUIRE_AUTH=true
DEBUG_HEADERS=false

# OpenMemory reference server
OPENMEMORY_BASE_URL=http://openmemory-api:8765
MCP_PUBLIC_BASE_URL=https://mcp.example.com/mcp
PRM_RESOURCE_URL=https://mcp.example.com/mcp
OIDC_ISSUER=https://keycloak.example.com/auth/realms/example
OIDC_AUDIENCE=https://mcp.example.com/mcp
ALLOWED_ORIGINS=https://chatgpt.com,https://chat.openai.com

# TODO: add per-service sections once additional MCPs are scaffolded
