# Root workspace .env example (optional)
# Compose flows rely on per-service .env files under services/<name>/.env.
# Populate this file only if you need to share values across multiple services
# (e.g., KEYCLOAK host overrides). Leave empty otherwise.

# Traefik routing host for services/github-mcp
# GITHUB_MCP_PUBLIC_HOST=github-mcp.onemainarmy.com
